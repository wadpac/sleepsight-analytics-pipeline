preprocess = function(studyfolder,desiredtz) {
  # identify folders
   list.dirs(studyfolder, recursive=FALSE)
  # unzip if necessary
  
  # print how many folders found
  
  # loop over folder and go inside
  
  # identify available information
  
  # print available information
  
  # process information and store in RData file per person
  
  
}